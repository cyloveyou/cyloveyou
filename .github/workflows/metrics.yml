name: GitHub Metrics (Manual)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Experimental metrics workflow is currently disabled for the profile README."
